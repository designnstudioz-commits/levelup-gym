// ZKTeco ADMS command-ack endpoint — machine calls /iclock/devicecmd directly
// This file exists alongside the rewrite in next.config.ts for maximum compatibility

export { GET, POST } from "@/app/api/attendance/devicecmd/route";
