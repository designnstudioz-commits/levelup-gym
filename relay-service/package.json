{
  "name": "zkteco-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone ADMS relay for ZKTeco devices — runs on a separate always-on VM, bypassing Vercel's bot protection for device traffic. See CLAUDE.md / plan history for context.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "@supabase/supabase-js": "^2.45.0"
  }
}
